<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css">
  <title>Recipe Ideas</title>
</head>

<body>
  <div class="container">
    <h1>Let's find a recepie for you ✌️</h1>

    <form action="/" id="form" class="form" method="POST">
      <select name="cuisine" class="form-select">
        <option value="" data-display="Select">Random cuisine</option>
        <option value="Italian" <%= selectedCuisine === 'Italian' ? 'selected' : '' %>>Italian</option>
        <option value="Asian" <%= selectedCuisine === 'Asian' ? 'selected' : '' %>>Asian</option>
        <option value="American" <%= selectedCuisine === 'American' ? 'selected' : '' %>>American</option>
        <option value="Mexican" <%= selectedCuisine === 'Mexican' ? 'selected' : '' %>>Mexican</option>
        <option value="Mediterranean" <%= selectedCuisine === 'Mediterranean' ? 'selected' : '' %>>Mediterranean</option>
        <option value="Pakistani" <%= selectedCuisine === 'Pakistani' ? 'selected' : '' %>>Pakistani</option>
        <option value="Japanese" <%= selectedCuisine === 'Japanese' ? 'selected' : '' %>>Japanese</option>
      </select>
      <select name="difficulty" class="form-select">
        <option value="">Random Difficulty</option>
        <option value="Easy" <%= selectedDifficulty === 'Easy' ? 'selected' : '' %>>Easy</option>
        <option value="Medium" <%= selectedDifficulty === 'Medium' ? 'selected' : '' %>>Medium</option>
      </select>
      <button type="submit" class="form-submit">Go</button>
    </form>
    <section id="cards" class="cards">
      <% if (recipe) { %>
        <article class="card-item">
          <h2 class="card-activity">
            <!-- Show Recepie -->
            <%= recipe.name %>
          </h2>
          <div class="card-info">
            <span class="card-type">
              <!-- Show Cuisine -->
              <%= recipe.cuisine %>
            </span>
            <span class="card-participants">
              <!--  Show difficulty -->
              <%= recipe.difficulty %>
                <span class="card-number"></span>
            </span>
          </div>
        </article>
        <% } %>

          <% if (error) { %>
            <div id="tag-error" class="tag-error">
              <%= error %>
                <!-- Show any errors that were sent from the backend -->
            </div>
            <% } %>

    </section>
  </div>

</body>

</html>